import { Vault } from "../types.js";
/**
 * Generates a new RSA key pair of the given key size.
 * @param keySize Key size.
 * @returns RSA key pair.
 */
export declare function generateRsaKeyPair(keySize?: number): Promise<{
    publicKey: string;
    privateKey: string;
}>;
/**
 * Decrypt cipher text using the given asymmetric private key.
 * @param privateKey Asymmetric private key.
 * @param cipherText Cipher text.
 * @param oaepHash Hash function to use for OAEP padding and MGF1.
 * @param padding Padding value.
 * @returns Decrypted text.
 */
export declare function asymmetricDecrypt(privateKey: string, cipherText: Buffer, oaepHash?: string, padding?: number): string;
export declare enum KEMHashAlgorithm {
    SHA512 = "sha512"
}
export declare enum KEMSymmetricAlgorithm {
    AES256_GCM = "AES-GCM-256"
}
export declare enum KEMkdf {
    PBKDF2 = "pbkdf2"
}
export declare function kemDecryptExportResult(result: Vault.ExportResult, password: string, privateKey: string): Promise<string>;
